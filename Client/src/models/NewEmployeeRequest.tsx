export interface NewEmployeeRequest {
  firstName: string;
  lastName: string;
  jobTitle: string;
  email: string;
  homeAddress: string;
  postalCode: string;
  city: string;
  bankAccountNumber: string;
  taxRate: number;
  monthlySalary: number;
  startDate: Date;
  dateOfBirth: Date;
}
